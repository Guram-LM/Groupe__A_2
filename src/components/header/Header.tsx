
import HeaderButton from "./HeaderButton"
import Logo from "./Logo"

const Header = () => {
  return (
    <header>
        <Logo/>
        <HeaderButton/>
    </header>
  )
}

export default Header